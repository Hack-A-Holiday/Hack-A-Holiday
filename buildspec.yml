version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18 # Or your project's Node.js version
    commands:
      - echo Installing NPM dependencies...
      - npm install
  build:
    commands:
      - echo Build started on `date`
      - echo No build commands needed for this app...
artifacts:
  files:
    - '**/*'
